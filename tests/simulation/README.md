# Simulation Test Area

This directory is reserved for topology discovery simulation and higher-level protocol validation.

Expected future uses:

- synthetic tile layout fixtures
- rescan regression tests
- asymmetric edge report tests
- duplicate UID tests
- coordinate derivation validation

Simulation should focus on deterministic discovery behavior before hardware-in-the-loop coverage is complete.
